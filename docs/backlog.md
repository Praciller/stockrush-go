# Backlog

Add only when the MVP evidence justifies the cost:

1. Protect public admin/demo endpoints with authentication and roles.
2. Add a shared rate limiter for horizontally scaled API replicas.
3. Expand OpenAPI schemas and generate a typed client.
4. Persist load-run summaries for historical comparison.
5. Add payment refunds and explicit settlement events.
6. Add a production deployment example after selecting a provider with stable free limits.

Redis, Kafka, Kubernetes, and microservices remain out of scope until measured scale requires them.
